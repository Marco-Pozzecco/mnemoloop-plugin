import type { FlashcardBaseContent } from '@/schemas';
import type { FlashcardContentProps } from '../types';

type ReviewFlashcardBasicProps = FlashcardContentProps<FlashcardBaseContent>;

export default ReviewFlashcardBasicProps;
