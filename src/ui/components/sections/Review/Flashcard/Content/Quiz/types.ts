import type { FlashcardQuizContent } from '@/schemas';
import type { FlashcardContentProps } from '../types';

type ReviewFlashcardQuizProps = FlashcardContentProps<FlashcardQuizContent>;

export default ReviewFlashcardQuizProps;
